name: Build:

```yaml
name: Build and Publish

on and Publish

on:
  release:
    types: [created] :
  release:
    types: [created]  # Runs when a new # Runs when a new release is created
  workflow_dispatch: release is created
  workflow_dispatch:  # Allows manual  # Allows manual triggering of the workflow

jobs:
 triggering of the workflow

jobs:
  build_wheels:
  build_wheels:
    name: Build wheels on ${{ matrix    name: Build wheels on ${{ matrix.os }}
    runs.os }}
    runs-on: ${{ matrix.os }}
    strategy:
     -on: ${{ matrix.os }}
    strategy:
      matrix:
        matrix:
        os: [ubuntu-latest, windows-l os: [ubuntu-latest, windows-latest, macos-latestatest, macos-latest]

    steps:
      - name: Check out code]

    steps:
      - name: Check out code
        uses
        uses: actions/checkout@v4

      - name: actions/checkout@v4

      - name: Set up Python: Set up Python
        uses: actions/setup-python@
        uses: actions/setup-python@v4
        withv4
        with:
          python-version: '3.13'

      - name: Install glibc (Linux only)
       :
          python-version: '3.13'

      - name: Install run: sudo apt-get update && sudo apt-get install - glibc (Linux only)
        run: sudo apt-get update && sudo apt-get install -y libc6-dev
        if:y libc6-dev
        if: runner.os == 'Linux'

      - name: Install cibuildwheel
        run: python -m pip runner.os == 'Linux'

      - name: Install cibuildwheel
        run: python -m pip install cibuildwheel

 install cibuildwheel

      - name: Build wheels
             - name: Build wheels
        run: python -m cibuildwheel --output-dir wheelhouse
        env:
          run: python -m cibuildwheel --output-dir wheelhouse
        env:
          CIBW_BUILD: "cp38-* cp39 CIBW_BUILD: "cp38-* cp39-* cp310-* cp311-* cp-* cp310-* cp311-* cp312-* cp313-*"
          CIBW_SKIP312-* cp313-*"
          CIBW_SKIP: "*-win32 *-many: "*-win32 *-manylinux_i686 *-manylinux1_i686"
linux_i686 *-manylinux1_i686"
          CIBW_ENVIRONMENT:          CIBW_ENVIRONMENT: "CXXFLAGS=-std=c++17"

      - name: Rename wheels with release version
 "CXXFLAGS=-std=c++17"

      - name: Rename wheels with release version
        shell: bash
        run        run: |
          $wheels = Get-ChildItem -Path ./: |
          for file in ./wheelhouse/*.whl; do
           wheelhouse/*.whl
          foreach ($wheel in $wheels) {
 newfile=$(echo $file | sed "s/\(.*\)\(\.whl\)/\1-${{ github.event.release.tag_name }}\2/")
            mv "$file" "$newfile"
          done

            $newName = $wheel.FullName      - name: Upload wheels as artifacts
        uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ github.event.release.tag_name }}-${{ matrix.os }}
          path: ./wheelhouse/*.whl

  publish:
    name: Publish to PyPI
    needs: build_wheels  # Waits for the build_wheels job to complete
    runs-on: ubuntu-latest

    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install twine

      - name: Download built wheels
        uses: actions/download-artifact@v4
        with:
          name: wheels-${{ github.event.release.tag -replace "(.+)(\.whl)", "`$1-${{ github.event.release.tag_name }}$2"
            Rename-Item -Path $wheel.FullName -NewName $newName
          }
        shell:_name }}-${{ matrix.os }}
          path: dist

 pwsh

      - name: Upload wheels as artifacts
        uses      - name: Publish to PyPI
        env:
          TWINE_USERNAME: "__token__"
          TWINE_PASSWORD: "${: actions/upload-artifact@v4
        with:
          name{ secrets.PYPI_API_TOKEN }}"
        run: twine upload dist/* --repository-url https://upload.pypi.org/legacy/
